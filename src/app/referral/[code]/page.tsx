import { notFound } from 'next/navigation';

// Referral links are intentionally unavailable until they are backed by a
// durable, auditable programme rather than temporary runtime memory.
export default function ReferralCodePage() {
  notFound();
}
